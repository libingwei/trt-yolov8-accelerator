# 设置所有文本文件自动处理换行符
* text=auto

# 强制 shell 脚本使用 LF 换行符
*.sh text eol=lf

# 强制 Python 文件使用 LF 换行符
*.py text eol=lf

# 强制 C/C++ 文件使用 LF 换行符
*.cpp text eol=lf
*.h text eol=lf

# 忽略权限变化（可选）
* -text
